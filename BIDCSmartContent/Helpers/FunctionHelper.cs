
namespace BIDVSmartContent.Helpers
{
    public static class FunctionHelper
    {
        //private static IFunctionStore _functionStoreService = new FunctionStoreService();
        //public static UserMenuViewModel GetFunctionByUserId(decimal id)
        //{
        //    var model = new UserMenuViewModel();
        //    var functions = _functionStoreService.GetFunctionsByUserId(id);
        //    model.ListFunctionLever1 =
        //        functions.Where(x => x.FuncDisplay == "1" & x.FuncLevel == 1).OrderBy(x => x.FuncOrder).ToList();
        //    model.ListFunctionDisplay = functions.Where(x => x.FuncDisplay == "1").ToList();
        //    model.ListFunctionNotDisplay = functions.Where(x => x.FuncDisplay == "0").ToList();
        //    foreach (var item in model.ListFunctionLever1)
        //    {
        //        var func = new FunctionViewModel(){FuncName = item.FuncName};
        //        func.ChildFunctions =
        //            functions.Where(x => x.FuncParentId == item.FuncId).OrderBy(x => x.FuncOrder).ToList();
        //    }
        //    return model;
        //}
    }
}